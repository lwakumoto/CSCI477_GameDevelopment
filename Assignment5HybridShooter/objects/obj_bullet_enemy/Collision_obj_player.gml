/// @description Insert description here
// You can write your code in this editor

if (other.is_blue != is_blue || sprite_index = spr_bullet_green){ // if the color of the player doesn't match with the color of the bullet
	audio_play_sound(snd_damage,1,0);
	other.hp -= atk;
	instance_destroy(id)
}
